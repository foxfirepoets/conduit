# How Might We -- Agent-Only Marketing Channels for Conduit

## Scope Ladder

### Narrow (Tactical -- specific channel mechanics)
1. HMW get Conduit's MCP server.json to appear in every agent's tool-selection query?
2. HMW embed AIVS-Micro tokens into proof bundles so every export carries attribution?
3. HMW make `pip install conduit-browser` the default answer when an agent searches PyPI for "auditable browser"?

### Medium (Strategic -- discovery and adoption patterns)
4. HMW make Conduit the first result when any AI agent searches for "verifiable web automation"?
5. HMW turn every Conduit proof bundle into a marketing artifact that teaches the receiving agent about Conduit?
6. HMW create a dependency chain where other agent tools require conduit-browser as a prerequisite?
7. HMW position Conduit's cryptographic audit trail as the de facto trust layer for agent-to-agent transactions?

### Wide (Ecosystem -- market shaping)
8. HMW establish CSPF (Conduit Session Proof Format) as the standard proof format that all agent frameworks reference?
9. HMW make "proof of execution" a requirement that agents demand before paying other agents -- with Conduit as the only tool that provides it?
10. HMW create a world where agents that do NOT produce cryptographic proofs are considered untrustworthy by default?

## Selected 3 (defining the matrix dimensions)

**HMW #4** -- Drives Dimension 1 (Discovery Surface) and Dimension 3 (Distribution Mechanism): where and how agents find Conduit.

**HMW #5** -- Drives Dimension 2 (Marketing Artifact Type): what Conduit exports that markets itself.

**HMW #9** -- Drives Dimension 4 (Uniqueness Lever): why Conduit specifically, and why no substitute exists.
